---
export interface props {
  src: string;
  alt: string;
  href: string;
}

const { src, alt, href } = Astro.props;
---

<a class="col-4 col-md-3 col-lg-2 pt-3 pb-3 bg-image hover-zoom" href={href} target="_blank">
  <img class="img-fluid "src={src} alt={alt} />
</a>
